# History

This folder preserves older or more implementation-heavy documents that are still useful as reference material.

These files are not the primary public-facing docs for the repository anymore. They remain here because they capture important stages in the project's evolution and may still contain valuable detail.

If a document here is still useful conceptually, it should eventually be distilled into the active docs tree.

If a document here is implementation-oriented, it likely belongs in one of the domain repositories long term.
