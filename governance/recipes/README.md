# Recipes

Recipes are reusable multi-step playbooks that sit above tools and below workflows.

The current workbook explicitly names one recipe:

- `conceptual-mock` - the designer recipe used during stage 1 concept generation

No detailed recipe document has been created yet because the workbook does not define the recipe beyond its name, inputs, outputs, and purpose inside stage 1.
