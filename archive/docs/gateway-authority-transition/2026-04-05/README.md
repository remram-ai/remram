# Gateway Authority Transition Archive

This folder preserves the detailed Remram-side Moltbox/Gateway docs that were active before Gateway became the authoritative source for its own appliance domain.

Archived here on `2026-04-05`:

- detailed CLI contract material
- detailed operator workflow material
- detailed Gateway concept material
- Remram-side AI bootstrap material that described the old Moltbox contract directly

The active rule after this archive point is:

- use `moltbox-gateway` for the live Moltbox appliance/operator/CLI contract
- use `remram` for ecosystem framing, feature records, and cross-repo architecture context
