# External Schemas

`schemas/external/` is reserved for machine-readable third-party schemas that RemRam must conform to.

Convention:

- machine schemas use `.schema.json`

Planned examples include OpenClaw payload formats, Ollama responses, and Discord webhook payloads.
