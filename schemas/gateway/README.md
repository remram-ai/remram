# Gateway Schemas

`schemas/gateway/` is reserved for machine-readable runtime and control-plane schemas produced by the Moltbox gateway.

Convention:

- machine schemas use `.schema.json`

Planned examples include deployment events, runtime checkpoints, gateway history, and service state payloads.
