# Gateway Schemas

`schemas/gateway/` is reserved for runtime and control-plane schemas produced by the Moltbox gateway.

Planned examples include deployment events, runtime checkpoints, gateway history, and service state payloads.
